declare var readline_object: any;
declare var log: Function;

// Compilé depuis des fichiers typescript :)

import handleInput from "../../../lib/util/input";
import input, { num, nums, numsMat, tuples } from "../../../lib/util/transform";

async function main() {
	const handler = handleInput(readline_object);

	let n = await handler.num();

	return n;
}


main().then((result: any) => {
	if (result !== undefined) {
		console.log(result);
	}
});